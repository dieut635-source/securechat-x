<!-- SecureChat: chỉ đóng gói tiếng Anh (xem localeFilters trong app/build.gradle.kts).
     Danh sách này phải khớp với localeFilters, nếu không bộ chọn ngôn ngữ của Android sẽ
     liệt kê những ngôn ngữ không có trong APK và chúng đều rơi về tiếng Anh. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en"/>
</locale-config>
